# ملاحظات: الأرشفة السريعة + حماية الكود

## 1) أرشفة أسرع على جوجل
تمت إضافة:
- **`/robots.txt`** (مسار ديناميكي في `server.js`) — يسمح بزحف الصفحات العامة، ويمنع
  زحف `/api/`، `/admin`، `/manager`، `/chat`، `/group`، `/save`، `/shiziai`، `/support`، `/login`،
  ويُشير لملف الـ sitemap.
- **`/sitemap.xml`** (مسار ديناميكي) — يُولَّد تلقائياً من قاعدة البيانات: الصفحة الرئيسية،
  كل حساب عام (غير خاص وغير موقوف)، كل صفحة أعمال (Page)، وآخر 3000 منشور عام.
- **`<link rel="canonical">`** و **`<meta name="robots">`** تلقائياً بكل صفحة (عبر دالة `injectOG`
  الموجودة أصلاً بالمشروع)، مع منع فهرسة الصفحات الخاصة/الإدارية تحديداً (`noindex`).
- **JSON-LD (بيانات منظّمة)** بصيغة `schema.org` على الصفحة الرئيسية (`WebSite`) وصفحات
  الملفات الشخصية (`ProfilePage` / `Person`) — تساعد جوجل على فهم المحتوى بشكل أفضل.

### خطوات يدوية بعد النشر (مهمة ولا يمكن أتمتتها من الكود)
1. افتح **Google Search Console**: https://search.google.com/search-console
2. أضف نطاق الموقع (Domain property أو URL prefix) وتحقق من الملكية.
3. من قسم **"خرائط الموقع" (Sitemaps)**، أضف: `https://YOUR-DOMAIN/sitemap.xml`
4. استخدم أداة **"فحص عنوان URL" (URL Inspection)** لطلب فهرسة الصفحة الرئيسية فوراً
   بدل انتظار الزحف التلقائي — هذا أسرع طريقة فعلية لتسريع الأرشفة.

## 2) منع نسخ/تقليد كود الواجهة
**تنبيه مهم وصريح:** أي موقع ويب — بما فيه هذا المشروع — يُرسل كود الـ HTML/CSS/JS
كاملاً لمتصفح الزائر ليعمل. هذا يعني أنه **لا توجد طريقة تقنية تمنع فتح "عرض المصدر"
أو تبويب الشبكة (Network) بشكل كامل ونهائي** — هذا قيد أساسي بطريقة عمل الويب نفسه،
وليس نقصاً بهذا الكود. أي إجراء "تعطيل زر يمين/F12" هو رادع بسيط للزوار العاديين فقط،
ويمكن لأي مطوّر متمرّس تجاوزه خلال دقائق.

### ما تمت إضافته كخطوط دفاع عملية:
- `public/protect.js` — يمنع القائمة السريعة للزر الأيمن واختصارات فتح أدوات المطوّر
  الشائعة (F12, Ctrl+Shift+I/J/C, Ctrl+U)، ويطبع تنبيه حقوق ملكية بالكونسول (مفيد كدليل
  عند أي نزاع لاحق). مُضاف تلقائياً على كل صفحات الموقع عبر `injectOG`.
- رؤوس أمان HTTP (`X-Frame-Options`, `Content-Security-Policy: frame-ancestors 'self'`) —
  تمنع تضمين الموقع داخل `<iframe>` بمواقع أخرى (أسلوب شائع لنسخ الواجهة بسرعة).
- حدّ معدّل طلبات بسيط (Rate Limiting) على `/api/*` لإبطاء الزحف/النسخ الآلي الجماعي
  للبيانات. هذا خط دفاع إضافي وليس مطلقاً، خصوصاً على استضافة serverless (Vercel) حيث
  تُعاد تهيئة الذاكرة بين النسخ (instances).

### الحماية الحقيقية ضد "التقليد" ليست تقنية بل قانونية/تشغيلية:
1. **شروط استخدام (Terms of Service)** صريحة تمنع النسخ، الزحف الآلي، وإعادة النشر.
2. **حقوق الملكية الفكرية**: يمكن توثيق تاريخ إنشاء الكود (مثلاً عبر Git history أو
   جهة توثيق) لإثبات الملكية عند أي نزاع.
3. **مراقبة السوق**: البحث الدوري عن نسخ مطابقة للموقع (بالاسم/الشعار/نص مميز).
4. **بلاغ DMCA**: عند اكتشاف نسخة مسروقة فعلياً منشورة على استضافة أخرى، يمكن تقديم
   بلاغ لمزوّد الاستضافة أو لمحرك البحث لإزالتها/عدم فهرستها.
5. إن أردت طبقة أقوى تقنياً لاحقاً: تعمية/تصغير الكود (build step بـ Webpack/Vite
   لتحويل الكود لصيغة أصعب قراءة، لكنها تبقى قابلة للتفكيك)، أو نقل أي منطق حسّاس
   (خوارزميات، قواعد أعمال دقيقة) للخادم بدل الواجهة الأمامية بالكامل.
