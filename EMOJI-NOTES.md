# حزمة الإيموجي (Fluent Emoji)

المصدر: https://github.com/microsoft/fluentui-emoji (مايكروسوفت، رخصة MIT).

## طريقة العمل
- `public/emoji-catalog.json`: قائمة مبنية من ملفات `metadata.json` بالمستودع
  الأصلي (1284 إيموجي: الاسم، التصنيف، الكلمات المفتاحية بالإنجليزي، رمز اليونيكود،
  ومسار صورة نمط "Color" لكل إيموجي).
- **الصور نفسها لا تُخزَّن داخل المشروع** — تُجلب مباشرة من نسخة المستودع على
  jsDelivr (`https://cdn.jsdelivr.net/gh/microsoft/fluentui-emoji@main/assets/...`)
  وهي شبكة CDN عامة مجانية لاستضافة أي مستودع GitHub علني، هذا تفادى تضخيم حجم
  النشر بآلاف ملفات SVG/PNG (المستودع الأصلي كبير جداً).
- `public/emoji.js` (يُحمَّل تلقائياً بكل صفحة عبر `server.js`):
  - `EmojiFluent.render(html)` — يحوّل أي رمز إيموجي يوينكود داخل نص/HTML إلى
    `<img class="femoji">` بنفس تصميم Fluent، ليظهر بشكل متطابق لكل المستخدمين
    بغض النظر عن نظام تشغيلهم (بدل الاعتماد على خط الإيموجي المدمج بكل جهاز).
  - `EmojiFluent.attachButton(button, target)` — يربط أي زر بمنتقي إيموجي مع بحث
    وتصنيفات، يُدرج الرمز داخل `target` (يدعم textarea/input وأيضاً عناصر
    contenteditable).
- تم ربط الاستبدال التلقائي (`render`) داخل كل دوال عرض المحتوى الموجودة أصلاً
  بالمشروع (`linkifyContent` / `linkifyEscaped` / `linkifyV`) في كل الصفحات
  (المنشورات، التعليقات، رسائل الدردشة الفردية والجماعية)، فيعمل تلقائياً بلا
  حاجة لتعديل كل مكان عرض على حدة.
- زر فتح المنتقي (😀) مُضاف حالياً بـ: محرر المنشور الجديد (`profile.html`)،
  ودردشة الأفراد والمجموعات (`chat.js` / `group.js`). يمكن إضافته لأي صندوق نص
  آخر (مثل صندوق التعليقات) بنفس الطريقة: زر + استدعاء
  `EmojiFluent.attachButton(btn, inputEl)`.

## ملاحظة اعتماد على شبكة خارجية
بما أن الصور تُجلب من jsDelivr وقت التصفح، فهذا يعني الموقع يعتمد على توفّر تلك
الخدمة لعرض الإيموجي بشكله الرسومي (لو تعطّلت مؤقتاً، ستظهر أيقونة صورة مكسورة
بدل الرمز، لكن `alt` بها الرمز الأصلي كنص احتياطي). إذا رغبت لاحقاً بتخزين الصور
داخل الاستضافة نفسها بدل الاعتماد على CDN خارجي، يمكن تحميل مجلد `assets/*/Color`
من نفس المستودع ورفعه لمجلد `public/emoji-assets/` وتعديل `CDN_BASE` بملف
`emoji.js` ليشير لمسار محلي بدل jsDelivr.
